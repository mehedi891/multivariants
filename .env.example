# Example environment configuration for the MultiVariants website.
# Copy this file to `.env` and adjust values as needed.
# All values below are public CMS config (no secrets).

# CMS base URL (efoli-cms)
CMS_API_BASE_URL="https://efoli-cms.vercel.app"

# Single CMS site slug used for ALL public API calls.
SITE_SLUG="multivariants"

# Public CMS API paths
CONTACT_API_PATH="/api/public/contact"
SITE_CONFIG_API_PATH="/api/public/site-config"
BLOG_API_PATH="/api/public/posts"
OPINIONS_API_PATH="/api/public/opinions"
DOCS_API_PATH="/api/public/docs"
PARTNERS_API_PATH="/api/public/partners"
CLIENTS_API_PATH="/api/public/clients"
CHANGELOG_API_PATH="/api/public/changelogs"
FAQ_API_PATH="/api/public/faqs"

# Blog API options
BLOG_API_LOG_ERRORS=true
BLOG_API_FORWARD_AUTH=false
BLOG_API_POSTS_URL="https://efoli-cms.vercel.app/api/public/posts"
BLOG_API_BASE_URL="https://efoli-cms.vercel.app"

# IndexNow — served key file lives at public/<INDEXNOW_KEY>.txt.
# On production deploys, postbuild submits the sitemap URLs to IndexNow.
INDEXNOW_KEY="a3f1c9e07b2d48f6a1c5e9d3b7028f4c"
INDEXNOW_HOST="multivariants.com"

# On-demand revalidation webhook secret — must match the CMS REVALIDATE_SECRET exactly.
REVALIDATE_SECRET=""

# Draft preview shared secret — must match the CMS PREVIEW_SECRET exactly.
# Sent to the CMS as the `x-preview-token` header on /api/public/posts/{slug} to
# unlock unpublished drafts, and used to authenticate the CMS "Preview on site"
# redirect to /api/preview.
# SERVER-SIDE ONLY — this token unlocks every unpublished draft on every site.
# Never prefix it with NEXT_PUBLIC_ and never reference it in client components.
PREVIEW_SECRET=""

# Analytics (public IDs). Set these ONLY in the Vercel *Production* environment
# so staging/preview and local dev never send tracking. Leave empty everywhere
# else. Analytics also only load when NODE_ENV=production.
#   - GA must be a GA4 measurement ID (G-XXXXXXXXXX). Universal Analytics (UA-…)
#     was shut down on 2023-07-01 and will NOT collect data.
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_FB_PIXEL_ID=""

# Tawk.to live chat embed URL. Optional — the app already defaults to the
# MultiVariants property. Override only to point a different site at its own
# Tawk.to widget. Chat loads in production only, lazily (on first interaction).
NEXT_PUBLIC_TAWK_SRC=""
